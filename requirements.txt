# Streamlit Community Cloud: keep this file minimal — heavy ML deps are in requirements-ml.txt
# (avoids long source builds / resolver failures on new Python versions).
# API / Render: pip install -r requirements-api.txt
# Training: pip install -r requirements-training.txt
setuptools>=69.0.0
wheel>=0.42.0
protobuf>=3.20,<6
streamlit==1.40.2
plotly==5.24.1
pandas>=2.2.3,<3
numpy>=1.26.4,<2
httpx==0.28.1
pydantic>=2.10,<3
pydantic-settings>=2.6,<3
