# ML + data stack for FastAPI, local export fallback, training, tests (not needed on Streamlit Community Cloud)
# (pyarrow is pulled in by Streamlit/pandas when needed — no separate pin.)
scikit-learn==1.5.2
xgboost==2.1.3
holidays==0.61
loguru==0.7.3
