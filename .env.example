# Paths (relative to project root recommended)
DATA_PATH=data/ferry_tickets.csv
MODELS_DIR=models
REPORTS_DIR=reports/figures
LOGS_DIR=logs

# Optional: require X-API-Key header when set
# API_KEY=your-secret-key

# Comma-separated origins for CORS
CORS_ORIGINS=http://localhost:8501,http://127.0.0.1:8501

# Optional Streamlit app password (if set in secrets.toml as APP_PASSWORD)
# APP_PASSWORD=

# Training script only (optional)
# FERRY_TRAIN_MAX_ROWS=40000
# SKIP_SARIMA=1
